import styled from "styled-components";

export const AuthorContainer = styled.span`
    font-size: 0.6rem;
    position: fixed;
    bottom: 15px;
    left: 15px;

    a {
        cursor: pointer;
    }
`;